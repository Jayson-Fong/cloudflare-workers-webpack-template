{
  "private": true,
  "name": "blog",
  "version": "1.0.0",
  "description": "A template for Cloudflare Workers project using webpack",
  "main": "dist/worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "webpack --progress",
    "preview": "./scripts/open-preview < dist/worker.js"
  },
  "author": "Jayson Fong <contact@jaysonfong.org>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  }
}
